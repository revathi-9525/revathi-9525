name: Generate Snake

on:
workflow_dispatch:

jobs:
build:
runs-on: ubuntu-latest

```
steps:
  - uses: Platane/snk@v3
    with:
      github_user_name: revathi-9525
      outputs: dist/github-contribution-grid-snake.svg
```
